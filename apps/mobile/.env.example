# 默认从 Expo hostUri 推导 http://<开发电脑 IP>:3000。
EXPO_PUBLIC_USE_DEV_HOST=true

# 连接指定后端时，将上方开关设为 false，再填写完整 origin。
# EXPO_PUBLIC_API_BASE_URL=https://api.example.com

# 兼容构建期入口配置：BASE_URL 优先，否则使用 https://${VERCEL_URL}。
# EXPO_PUBLIC_API_BASE_URL 的优先级高于此配置。
# BASE_URL=http://localhost:3000
