# 复制到 apps/web/.env.local，填写当前应用的配置。
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require
# 使用 openssl rand -base64 32 生成，不要提交真实密钥。
BETTER_AUTH_SECRET=
# Vercel 部署可省略 BASE_URL，默认使用 https://${VERCEL_URL}。
BASE_URL=http://localhost:3000
